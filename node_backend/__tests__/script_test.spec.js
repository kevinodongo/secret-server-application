describe("test encryption and decryption funtions",  () => {
    beforeAll(done => {
      done()
    })
    
    afterAll(done => {
      done()
    })
  
    it("test", done => {
       expect(1).toBe(1)
       done()
    })
  })